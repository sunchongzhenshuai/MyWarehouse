<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.3.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

	<!-- 自动扫描的方式 -->
	<context:component-scan base-package="com.st.rbac.controller"></context:component-scan>
	
	<!-- 注解 -->
	<mvc:annotation-driven></mvc:annotation-driven>
	
	<!-- 视图解析器 -->
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<!-- 前缀 -->
		<property name="prefix" value="/"></property>
		<property name="suffix" value=".jsp"></property>
	</bean>
		<!-- 配置拦截器们 -->
	<mvc:interceptors>
		<!-- 配置一个具体的拦截器 -->
		<mvc:interceptor>
			<!-- 要拦截的操作 -->
			<mvc:mapping path="/**"/>
			<!-- 设置需要排除的操作 -->
			<mvc:exclude-mapping path="/Staff/login.action"/>
			<!-- 该 拦截器所对应类 -->
			<bean class="com.st.rbac.interceptor.StaffInterceptor"></bean>
		</mvc:interceptor>
		<mvc:interceptor>
			<!-- 要拦截的路径 -->
			<mvc:mapping path="/role/**/"/>
			<!-- 拦截器类 -->
			<bean class="com.st.rbac.interceptor.RoleInterceptor"></bean>
			
		</mvc:interceptor>
	</mvc:interceptors>
	<!-- 文件解析器 -->
	<bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<!-- 设置上传文件的大小 -->
		<property name="maxUploadSize">
		<value>#{10*1024*1024}</value>
		</property>
	</bean>
	
</beans>
